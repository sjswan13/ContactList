import React from 'react';

export default function (setSelectedContactId) {
  return (
    <div>
      <tr>
      <td>{contact.name}</td>      
      <td>{contact.email}</td>      
      <td>{contact.phone}</td>      
    </tr>
    </div>
  )
}